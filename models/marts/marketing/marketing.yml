version: 2

models:
  - name: dim_customers
    description: Staged customer data from our jaffle shop app
    columns:
      - name: customer_id
        description: The primary key for customers.
        tests:
          - unique
          - not_null

  - name: fct_orders
    description: Staged customer data from our jaffle shop app
    columns:
      - name: customer_id
        description: The primary key for customers.
        tests:
          - unique
          - not_null