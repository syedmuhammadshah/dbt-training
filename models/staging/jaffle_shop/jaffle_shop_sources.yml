version: 2

sources:
  - name: jaffle_shop
    database: nodal-album-438010-r2
    schema: raw
    tables:
      - name: raw_customers
        columns:
          - name: customer_id
            tests:
              - unique
              - not_null
      - name: raw_orders
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: raw_payments
